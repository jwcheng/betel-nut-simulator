# Copy to .env.local. SERVER-SIDE key — no VITE_ prefix, so it never enters the
# client bundle. Get one at https://openrouter.ai/keys.
# Live AI in local dev requires `vercel dev` (plain `npm run dev` = offline mode).
OPENROUTER_API_KEY=sk-or-...